/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/AI-Film-Studio',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
}

module.exports = nextConfig
